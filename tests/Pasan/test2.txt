// Usa una variable global y una local para calcular un resultado.
int main() {
    int base = 5;
    int factor = 2;
    base = base * factor; // Usa tanto la local como la global.
    return base;
}