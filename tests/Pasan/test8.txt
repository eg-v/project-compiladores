// Una expresión booleana más elaborada.
int main() {
    bool a = true;
    bool b = false;
    bool c = true;

    // resultado = (true AND true) OR (false AND !false) -> true OR false -> true
    bool resultado = (a AND c) OR (b AND !b);

    return 0;
}